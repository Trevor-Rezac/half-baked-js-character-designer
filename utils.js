export function makeStatsString(headCount, middleCount, bottomCount) {
    return `You have changed the head ${headCount} times, the middle ${middleCount} times, and the bottom ${bottomCount} times!`;
}